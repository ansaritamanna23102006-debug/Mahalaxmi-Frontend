export const metadata = {
  title: 'Login',
  description: 'Log in to your Mahalaxmi Mithaiwala account to track orders and manage your wishlist.',
  robots: {
    index: false,
    follow: false,
  }
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
