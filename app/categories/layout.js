export const metadata = {
  title: 'Categories',
  description: 'Browse our complete catalog of traditional sweets, dry fruits, farsan, and festive gift collections.',
};

export default function CategoriesLayout({ children }) {
  return <>{children}</>;
}
